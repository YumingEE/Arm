第一操作数可以是立即数或寄存器
mov 直接传递	
mov r0, #12   // r0 = 12
mov r0, r1    // r0 = r1
	
mvn 取反传递
mvn r0, #0    // r0 = ~0x0 
mvn r0, r1    // r0 = ~r1

当存在第二操作数时第一操作数必须是寄存器;
当第一操作数和目标寄存器相同时可以省略;
对立即数的范围有要求,只能是由8bit连续有效位通过偶数次移位能得到的数;